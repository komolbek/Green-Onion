using System;
using GreenOnion;

namespace GreenOnion.Utilities
{
    // TODO: Maybe convert just to a function instead of using OOP?
    public class Validator
    {
        public Validator()
        {
        }

        public ValidationError validate()
        {
            return new ValidationError();
        }
    }
}
